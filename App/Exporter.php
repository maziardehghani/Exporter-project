<?phpinclude "config/config.php";abstract class Exporter implements Exportble{    protected $data;    protected $format;    public function __construct($data)    {        $this->validation($data);        $this->data = $data;    }    protected function validation($data)    {        if (empty($data))        {            echo 'the Information can not be empty';            die();        }    }}